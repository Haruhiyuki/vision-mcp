# Build artifacts
node_modules
dist
*.tsbuildinfo

# Generated during packages/cli prepublish packaging
/packages/cli/native/
/packages/cli/skills/
/packages/cli/examples/

# Editor / OS
.DS_Store
.vscode/settings.json
.idea
*.log

# Test / runtime
coverage
.vitest
trace.db
trace.db-journal

# Secrets / env
.env
.env.local

# vision-mcp runtime cache
.vision-mcp/runtime-cache

# 设计文档草稿（本地保留，不入库）
vision_mcp_windows_macos_design.md

# ============ vision-mcp 运行时产物（不入库） ============
# 用户的 map data 目录（VISION_MCP_APPS_ROOT 默认指向）。参考 maps 在 examples/，
# 用户应自己 copy 一份到 apps/（或任意位置）作为工作目录。
/apps/

# 实测/CI 产生的 trace（包括 events / sessions / 截图 assets）
**/.traces/
# 探索阶段截图 / dump
**/.explore*/
**/.record/
**/.discover/
.explore/

# 临时 / 调试输出
/tmp/vmcp-*
*.tmp.png
*.tmp.json
packages/bench
# session_only patches（按定义不该入库；trusted/untrusted_proposal 可 commit）
# 注：当前没有自动按 trust 过滤的 hook；如果手动入库 session_only patch
# 要先把 trust 升级为 trusted，否则在干净 clone 上行为不一致。
